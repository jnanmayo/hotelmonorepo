import { HkTasksBoard } from '@/features/housekeeping/components/hk-tasks-board';

export const metadata = { title: 'Cleaning Tasks — Housekeeping' };

export default function HousekeepingTasksPage() {
  return <HkTasksBoard />;
}
