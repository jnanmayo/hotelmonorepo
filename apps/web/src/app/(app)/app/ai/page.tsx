import { CcAiPage } from '@/features/command-center/components/cc-sections';

export const metadata = { title: 'AI Command Center — TungaOS' };

export default function AiPage() {
  return <CcAiPage />;
}
