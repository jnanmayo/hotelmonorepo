import { ProcDashboard } from '@/features/procurement/components/proc-dashboard';

export const metadata = { title: 'Procurement — TungaOS' };

export default function ProcurementHomePage() {
  return <ProcDashboard />;
}
