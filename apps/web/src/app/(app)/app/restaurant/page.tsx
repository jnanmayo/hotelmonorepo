import { FnbDashboard } from '@/features/restaurant/components/fnb-dashboard';

export const metadata = { title: 'Restaurant POS — TungaOS' };

export default function RestaurantHomePage() {
  return <FnbDashboard />;
}
