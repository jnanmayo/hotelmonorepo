import { ArrivalBoard } from '@/features/front-desk/components/arrival-board';

export const metadata = { title: 'Arrivals — Front Desk' };

export default function ArrivalsPage() {
  return <ArrivalBoard />;
}
