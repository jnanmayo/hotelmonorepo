import { WalkInForm } from '@/features/front-desk/components/walk-in-and-rooms';

export const metadata = { title: 'Walk-in — Front Desk' };

export default function WalkInPage() {
  return <WalkInForm />;
}
