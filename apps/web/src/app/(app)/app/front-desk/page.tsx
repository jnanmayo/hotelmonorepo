import { FrontDeskDashboard } from '@/features/front-desk/components/front-desk-dashboard';

export const metadata = { title: 'Front Desk — TungaOS' };

export default function FrontDeskHomePage() {
  return <FrontDeskDashboard />;
}
