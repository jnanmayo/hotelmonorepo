import { PaymentCollectionPage } from '@/features/front-desk/components/folio-and-payments';

export const metadata = { title: 'Payments — Front Desk' };

export default function PaymentsPage() {
  return <PaymentCollectionPage />;
}
