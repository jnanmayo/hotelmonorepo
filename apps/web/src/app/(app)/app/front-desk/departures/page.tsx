import { DepartureBoard } from '@/features/front-desk/components/departure-board';

export const metadata = { title: 'Departures — Front Desk' };

export default function DeparturesPage() {
  return <DepartureBoard />;
}
