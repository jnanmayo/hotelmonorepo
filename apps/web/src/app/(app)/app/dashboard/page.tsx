import { CcDashboard } from '@/features/command-center/components/cc-dashboard';

export const metadata = { title: 'Command Center — TungaOS' };

export default function DashboardPage() {
  return <CcDashboard />;
}
