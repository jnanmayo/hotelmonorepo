import { RoomBoard } from '@/features/rooms/components/room-board';

export const metadata = { title: 'Room Status Board — TungaOS' };

export default function RoomsBoardPage() {
  return <RoomBoard />;
}
