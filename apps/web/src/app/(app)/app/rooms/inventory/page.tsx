import { RoomInventoryPage } from '@/features/rooms/components/room-inventory';

export const metadata = { title: 'Property Structure — TungaOS' };

export default function RoomsInventoryPage() {
  return <RoomInventoryPage />;
}
