import { RoomDashboard } from '@/features/rooms/components/room-dashboard';

export const metadata = { title: 'Room Management — TungaOS' };

export default function RoomsHomePage() {
  return <RoomDashboard />;
}
