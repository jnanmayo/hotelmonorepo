import { PmsReservationsList } from '@/features/pms/components/pms-reservations-list';

export const metadata = { title: 'Reservations — PMS' };

export default function ReservationsPage() {
  return <PmsReservationsList />;
}
