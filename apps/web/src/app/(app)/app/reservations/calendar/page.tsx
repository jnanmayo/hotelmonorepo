import { PmsCalendarView } from '@/features/pms/components/pms-calendar-view';

export const metadata = { title: 'Calendar — PMS' };

export default function ReservationsCalendarPage() {
  return <PmsCalendarView />;
}
