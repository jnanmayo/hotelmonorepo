import { InvDashboard } from '@/features/inventory/components/inv-dashboard';

export const metadata = { title: 'Inventory — TungaOS' };

export default function InventoryHomePage() {
  return <InvDashboard />;
}
