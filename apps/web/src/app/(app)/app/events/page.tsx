import { EventsDashboard } from '@/features/events/components/events-dashboard';

export const metadata = { title: 'Events — TungaOS' };

export default function EventsHomePage() {
  return <EventsDashboard />;
}
