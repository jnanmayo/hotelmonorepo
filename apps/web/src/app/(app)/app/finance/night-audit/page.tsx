import { PmsNightAuditPage } from '@/features/pms/components/pms-night-audit-page';

export const metadata = { title: 'Night Audit — PMS' };

export default function NightAuditPage() {
  return <PmsNightAuditPage />;
}
