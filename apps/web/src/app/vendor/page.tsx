import { VendorPortalPage } from '@/features/procurement/components/vendor-portal';

export const metadata = { title: 'Vendor Portal — TungaOS' };

export default function VendorPortalRoute() {
  return <VendorPortalPage />;
}
